/**
 * Thumbnail Worker
 *
 * This worker imports the thumbnail processing logic from the package.
 * It initializes wasm-vips and handles thumbnail generation requests.
 */
import "@livestore-filesync/image-thumbnails/worker"
